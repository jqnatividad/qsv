#!/bin/sh

version=$(git describe --abbrev=0 --tags)
name="qsv-$version-x86_64-unknown-linux-gnu"

./scripts/build-release
github-release upload --user jqnatividad --repo qsv --tag $version \
  --name "$name.tar.gz" \
  --file "./builds/$name.tar.gz"
